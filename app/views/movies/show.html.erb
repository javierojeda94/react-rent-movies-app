<%= react_component('Movie', props: { id: @movie.id,
                                      name: @movie.name,
                                      synopsis: @movie.synopsis,
                                      rating: @movie.rating,
                                      selfUrl: movie_path(@movie),
                                      editUrl: edit_movie_path(@movie),
                                      rentUrl: rent_movie_path,
                                      authenticityToken: form_authenticity_token }) %>